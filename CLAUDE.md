
## 기능 정의 = 3엔진 × 3OS (사용자 지시 2026-09-11)
기능 하나는 Claude Code·Codex·Gemini(agy)에서 같은 동작이어야 하고 맥·리눅스·윈도우(WSL2)에서 돌아가야 한다.
착수 전에 9칸 표(엔진 × OS, 필요하면 리눅스 컨테이너 열 추가)를 먼저 채워 보고하고, 안 되는 칸은 그때 이유와 범위 포함 여부를 말한다.
완료 보고도 이 표로만 한다. 특정 엔진·OS만 하는 건 사용자가 그렇게 정했을 때만.
윈도우는 WSL2를 뜻한다(네이티브 윈도우는 범위 밖, 별도 언급 불필요).
외부 도구 능력은 코드 주석이 아니라 실물(파일·바이너리·도움말)로 검증한 뒤 말한다.
